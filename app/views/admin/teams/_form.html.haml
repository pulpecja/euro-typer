= simple_form_for(@team, url: polymorphic_url([:admin, @team]), html: { class: 'form-horizontal' }, wrapper: :horizontal_form ) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :abbreviation
    = f.input :flag
    = f.input :photo

  .form-actions
    = f.button :submit, 'Zapisz drużynę'
